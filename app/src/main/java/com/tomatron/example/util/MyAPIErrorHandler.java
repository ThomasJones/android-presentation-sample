package com.tomatron.example.util;

/**
 * Injectable, mockable handler for API errors.
 */
public class MyAPIErrorHandler {

    public void handle(Throwable e) {
        // Doesn't do anything yet, just an example of an injectable, mockable dependency.
    }
}
